// forEach(), map(), filter(), find(), reduce(), some(), every(), from(), flatMap(), reduceRight(), keys(), entries(), with()

// forEach() creates a new array
const num = [1, 2, 3, 4, 5];
num.forEach((value, index, arr) => console.log(value * 2));

// map() creates a new array with return element
console.log(num.map((value, index, arr) => );